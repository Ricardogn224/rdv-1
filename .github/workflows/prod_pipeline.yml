name: PROD CD

on:
  push:
    branches:
      - production

  pull_request:
    branches:
      - production

jobs:
  install:
    name: Install and  run front 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          cd client
          npm i 
          nohup npm run dev &

  

  
      
        

          